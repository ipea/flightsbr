# Project Memory

Corrections and learned facts that persist across sessions for **flightsbr**.
When a mistake is corrected, or a non-obvious approach is confirmed, append a `[LEARN:category]` entry below.

Prior content (inherited from the workflow template's own development history, not about flightsbr) is archived at [`quality_reports/archive/inherited-template-memory.md`](quality_reports/archive/inherited-template-memory.md).

---

<!-- Append new entries below. Most recent at bottom. -->
